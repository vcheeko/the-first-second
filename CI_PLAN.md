# Prototype CI plan

The next engineering-quality step is to extract the v0.3 Seconds mechanics into a small pure module, test boundary/stability behavior with Node's built-in test runner, keep the browser prototype behavior unchanged, and run syntax/tests automatically in GitHub Actions.

This document is planning evidence only; it is not a claim that CI already exists.
